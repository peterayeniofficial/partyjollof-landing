/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["avatars.githubusercontent.com", "images.unsplash.com", "www.themoviedb.org", "www.nollywoodreinvented.com"],
      }
}

module.exports = nextConfig
